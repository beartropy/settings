<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Beartropy Settings
    |--------------------------------------------------------------------------
    |
    | Settings are now managed dynamically via the database.
    | You can manage them using the SettingsManager Livewire component.
    |
    */
    'settings' => [
        // Settings are stored in the `beartropy_settings` table.
    ],
];
